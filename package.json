{
  "name": "semver-features-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "vitest run",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.17.4",
    "@changesets/cli": "^2.28.1",
    "@vitest/coverage-v8": "^3.0.8",
    "@vitest/ui": "^3.0.8",
    "publint": "^0.3.9",
    "turbo": "^2.2.0",
    "vitest": "^3.0.8",
    "zod": "^3.24.2"
  },
  "packageManager": "npm@10.2.4"
}
