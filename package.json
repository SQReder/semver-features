{
  "name": "semver-features-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "@changesets/cli": "^2.28.1",
    "turbo": "^2.2.0",
    "vitest": "^3.0.8"
  },
  "packageManager": "npm@10.2.4"
}
